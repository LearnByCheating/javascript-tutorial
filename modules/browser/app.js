/* eslint-disable */
// To run this file, open the index.html file.
//   Right click anywhere in the file, then select run with LiveServer.
//   Go to the browser to URL: http://localhost:5501
import calculator from './calculator.mjs';

const res1 = calculator.double(10);
const res2 = calculator.triple(10);

console.log('Log results from calculator module:', res1, res2);
